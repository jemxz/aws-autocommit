{
  "name": "aws-autocommit",
  "version": "1.0.0",
  "description": "auto commit file for automated commited to ga github repo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jemail",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security",
    "node-cmd": "^5.0.0"
  }
}
